<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
   
    <title>Edit Book</title>
    <style>
        /* General Styles for Edit Page */
        body.edit-page {
            font-family: 'Arial', sans-serif;
            background-color: #f5f3ff; /* Light grayish background */
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
        }

        /* Main Heading */
        h1 {
            font-size: 2.5rem;
            color: #2c3e50; /* Subtle dark blue-gray */
            text-align: center;
            margin-bottom: 20px;
        }

        /* Form Container */
        #edit-book-form {
            background-color: #ffffff; /* Clean white background */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Soft shadow */
            max-width: 400px;
            width: 100%;
        }

        /* Form Fields */
        #edit-book-form div {
            margin-bottom: 15px;
        }

        #edit-book-form label {
            display: block;
            font-weight: bold;
            color: #34495e; /* Darker gray-blue for labels */
            margin-bottom: 5px;
        }

        #edit-book-form textarea,
        #edit-book-form input[type="number"] {
            width: 100%;
            padding: 10px;
            font-size: 1rem;
            border: 1px solid #dcdde1; /* Light gray border */
            border-radius: 5px;
            outline: none;
        }

        #edit-book-form textarea {
            resize: none;
            height: 120px;
        }

        /* Submit Button */
        #edit-book-form button {
            display: block;
            width: 100%;
            padding: 10px;
            font-size: 1.2rem;
            background-color: #3498db; /* Soft blue */
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        #edit-book-form button:hover {
            background-color: #2980b9; /* Slightly darker blue on hover */
        }

        /* Back to Home Link */
        a {
            text-decoration: none;
            color: #3498db; /* Matches button color */
            font-size: 1rem;
            margin-top: 15px;
            display: inline-block;
            transition: color 0.3s ease;
        }

        a:hover {
            color: #2980b9; /* Matches button hover color */
        }

        /* Responsive Design */
        @media (max-width: 480px) {
            h1 {
                font-size: 2rem;
            }
            #edit-book-form {
                padding: 15px;
            }
            #edit-book-form button {
                font-size: 1rem;
            }
        }
        
    </style>
</head>
<body class="edit-page">
    <h1>Edit Book</h1>
    <form id= "edit-book-form" action="/books/<%= book.id %>?_method=PATCH" method="POST">
        <div>
            <label for="description">Description:</label>
            <textarea name="description" id="description" required><%= book.description %></textarea>
        </div>
        <div>
            <label for="rating">Rating:</label>
            <input type="number" name="rating" id="rating" value="<%= book.rating %>" min="1" max="5" required>
        </div>
        <button type="submit">Update Book</button>
    </form>
    <a href="/">Back to Home</a>
</body>
</html>
