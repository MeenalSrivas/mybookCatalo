<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Book Details</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Style for the entire page */
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #907a9e; /* Soft background color */
      color: #333; /* Dark text color */
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
    }

    /* Style for the book details card */
    .book-details-card {
      background-color: #804444;
      border: 1px solid #ddd;
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      padding: 20px;
      max-width: 400px;
      text-align: center;
    }

    /* Style for the book cover image */
    .book-details-card img {
      width: 100%;
      height: auto;
      border-radius: 4px;
      margin-bottom: 20px;
    }

    /* Style for the book title */
    .book-details-card h1 {
      font-size: 24px;
      margin: 10px 0;
      color: #2c3e50; /* Darker blue-gray for the title */
    }

    /* Style for the description */
    .book-details-card p {
      font-size: 16px;
      margin: 10px 0;
      line-height: 1.5;
    }

    /* Style for the back button */
    .book-details-card a {
      display: inline-block;
      margin-top: 15px;
      text-decoration: none;
      color: #ffffff;
      background-color: #3498db; /* Blue color */
      padding: 10px 20px;
      border-radius: 4px;
      font-size: 16px;
      font-weight: bold;
    }

    .book-details-card a:hover {
      background-color: #2980b9; /* Darker blue for hover */
    }
  </style>
  

</head>
<body id="book-details-page">
  <div class="book-details-card">
    
    <img src="https://covers.openlibrary.org/b/olid/<%= book.olid %>-M.jpg" alt="Book Cover">
    <h1><%= book.title %></h1>
    <p><strong>Description:</strong> <%= book.description %></p>
    <p><strong>Rating:</strong> <%= book.rating %> / 5</p></a>
    <a href="/">Back to Home</a>
    <div><a href="/books/<%= book.id %>/delete" class="btn btn-primary">Delete</a></div>

    
    
  </div>
</body>
</html>
